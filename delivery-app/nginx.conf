server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # This is required for React Router to work.
    # Without it, refreshing any page gives a 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}